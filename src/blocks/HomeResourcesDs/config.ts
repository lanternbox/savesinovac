import { Block } from "payload";

export const HomeResourcesDs: Block = {
  slug: "HomeResourcesDs",
  fields: [
    {
      name: "heading",
      type: "text",
      localized: true,
    },
  ],
};
